<nav class="bg-white border-b border-gray-200 py-4">
  <div class="container w-full h-auto mx-auto flex flex-wrap items-center justify-between px-6 lg:px-0 text-sm font-medium" data-controller="toggle">
    <div class="flex items-center flex-shrink-0 mr-6">
      <%= link_to root_path, class: 'flex' do %>
        <span class="text-3xl text-blue-700 hover:text-blue-800">Projects</span>
      <% end %>
    </div>

    <div class="block lg:hidden">
      <button type="button" id="sidebar-open" class="flex items-center p-3 text-gray-500 lg:hidden focus:outline-none focus:text-gray-700" data-action="click->toggle#toggle touch->toggle#toggle">
        <svg class="w-5 h-5 fill-current" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path></svg>
      </button>
    </div>

    <div class="flex-grow hidden w-full pb-6 lg:flex lg:w-auto lg:pb-0" data-toggle-target="toggleable">

      <div>
        <% if user_signed_in? %>
          <%= link_to 'Log out', destroy_user_session_path, data: {turbo_method: :delete},
                      class: "ml-2 rounded-lg py-3 px-5 bg-red-100 inline-block font-medium" %>
          <%= link_to 'Projects List', projects_path,
                      class: "ml-2 rounded-lg py-3 px-5 bg-blue-100 inline-block font-medium" %>
        <% else %>
          <%= link_to 'Log in', new_user_session_path,
                      class: "ml-2 rounded-lg py-3 px-5 bg-green-100 inline-block font-medium"%>
          <%= link_to 'Sign up', new_user_registration_path,
                      class: "ml-2 rounded-lg py-3 px-5 bg-blue-100 inline-block font-medium"%>
        <% end %>
      </div>
    </div>
  </div>
</nav>